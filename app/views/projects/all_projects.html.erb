<section class="all-projects">
  <% @all_projects.each do |project| %>
    <section class="mx-auto my-5 project-card">

      <div class="card">
        <div class="card-body d-flex flex-row">
          <% if project.user.avatar.attached? %>
            <%= image_tag project.user.avatar.url, class: "rounded-circle me-3", width: 50, height: 50 %>
          <% end %>
          <div>
            <h5 class="card-title font-weight-bold mb-2">
              <%= link_to project.title, project %>
            </h5>
            <p class="card-text"><i class="far fa-clock pe-2"></i><%= project.end_date.strftime("%B %d, %Y") %></p>
            <p class="user-name fa fa-user">
                <%= project.user.user_name %>
            </p>
          </div>
        </div>
        <div class="bg-image hover-overlay ripple rounded-0" data-mdb-ripple-color="light">
          <%= image_tag project.thumbnail(0), class: "img-fluid" %>
          <a href="#!">
            <div class="mask" style="background-color: rgba(251, 251, 251, 0.15);"></div>
          </a>
        </div>
        <div class="card-body">
          <p class="card-text collapse" id="collapseContent-<%= project.id %>">
            <%= project.description %>
          </p>
          <div class="d-flex justify-content-between">
            <a class="btn btn-link link-danger p-md-1 my-1" data-mdb-toggle="collapse" href="#collapseContent-<%= project.id %>"
              role="button" aria-expanded="false" aria-controls="collapseContent-<%= project.id %>">Read more</a>
          </div>
        </div>
      </div>
    </section>
  <% end %>
</section>


